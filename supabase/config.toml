# Supabase CLI local development configuration for Ask Genie Bhai.
# Reference: https://supabase.com/docs/guides/local-development/cli/config
#
# This project ID is a local identifier only — it does not need to match
# the remote project ref, and no remote project ref is recorded here.
# Link this local checkout to the remote `askgeniebhai` Supabase project
# with `supabase link`, using credentials that are never committed.
project_id = "askgeniebhai"

[api]
enabled = true
port = 54321
schemas = ["public", "graphql_public"]
extra_search_path = ["public"]
max_rows = 1000

[db]
port = 54322
major_version = 17

[studio]
enabled = true
port = 54323

[auth]
enabled = true
site_url = "http://localhost:3000"
additional_redirect_urls = []
jwt_expiry = 3600
enable_signup = true

[auth.email]
enable_signup = true
enable_confirmations = true
