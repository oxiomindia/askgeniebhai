import type { NextConfig } from "next";

// No-op comment: forces a fresh Vercel build/deploy of current main to
// rule out a stale-deployment explanation for a reported runtime error.
// Not a configuration change.
const nextConfig: NextConfig = {/* config options here */};

export default nextConfig;
